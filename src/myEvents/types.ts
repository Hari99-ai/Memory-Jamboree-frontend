// types.ts
export type EventDetailsResponse = {
  event_name: string;
  event_start: string;
  event_end: string;
  estatus: number;
};
// types.ts
export type Discipline = {
  id: string;
  name: string;
  description: string;
  startTime: string;
  // other relevant properties
};
